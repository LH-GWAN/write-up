# 브루트 포싱
1. 소스코드를 확인해보면 서버가 실행될 때 0부터 10000 사이의 랜덤한 숫자 하나가 TARGET_NUMBER로 설정된다.
2. /guess 엔드포인트에 POST 요청으로 guess 값을 전달했을 때, 이 값이 TARGET_NUMBER와 같으면 플래그가 반환된다.
3. TARGET_NUMBER는 매 요청마다 리프레시 되는게 아니다. 한번만 생성된다
4. 브루트 포싱을 통해 플래그 획득이 가능하다
