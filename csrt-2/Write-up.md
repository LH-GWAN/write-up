# XSS + CSRF 문제가 결합된 문제 유형이다
2. /flag 코드를 보면 admin session을 생성 후, 해당 세션을 봇에 넣고 사용자가 입력한 param 값이 들어 있는 /vuln 페이지에 접속시킨다
3. /vuln에서는 사용자가 입력한 값을 그대로 반환한다.
4. "frame", "script", "on" 해당 문자들은 제한된다.
5. 때문에 특정 코드를 봇에 넣을 경우 해당 봇은 admin 권한이 있는 상태이기에 admin 권한으로 해당 명령어를 실행한다.
6. 실행된 명령어를 가지고 admin 비밀번호를 바꾼 후, 바꾼 비밀번호로 admin 게정에 접속하면 flag가 추출된다.
