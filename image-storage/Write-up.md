1. 업로드 코드에 사용자의 파일을 검사하는 코드가 없다는 점을 확인했다
2. 그렇기 때문에 .php 파일을 올려 웹셸을 실행할 수 있다는 점을 확인했다
3. 이후 shell.php 파일을 올려 웹셸을 실행할 수 있는 코드를 올리고(다른 파일 참조) 해당 파일을 실행하였다
4. URL에 ?cmd='파라미터'를 통해 웹셸에 파라미터를 전달하여 flag 파일을 획득하게 되었다.
